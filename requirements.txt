requests>=2.31.0
python-dotenv>=1.0.1
python-dateutil>=2.9.0
tzdata>=2024.1
# py-clob-client is required ONLY for live trading. Install it explicitly
# with `pip install py-clob-client` or via the `[live]` extra in pyproject:
#     pip install -e .[live]
